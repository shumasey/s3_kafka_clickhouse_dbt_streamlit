export click_host=37.139.35.197
export click_user=alexey_shumakov
export click_pass=5F8LFuwQrhZkT56d
export kafka_host=kafka.ijklmn.xyz
export kafka_port=9092

for sql in sql_scripts/*.sql; do
    envsubst < "$sql" | clickhouse-client \
    -h "$click_host" \
    -u "$click_user" \
    --password "$click_pass" \
    --multiline
done
